# Baseline versus DAPR validation report

The active fair-comparison scope is now:

- eleven controlled baseline segmentation models;
- one proposal method: **DAPR U-Net**.

Older spectral proposal methods are intentionally removed from the active comparison scope. The repository includes audit tools to verify that all fair-comparison configs share the same data, optimization, loss, epoch, threshold, and evaluation settings.
