# Baseline audit

This repository keeps eleven controlled baseline implementations for fair comparison with **DAPR U-Net**.

The active proposal scope contains only `dapr_unet`. Older spectral proposal variants are intentionally not part of the manuscript comparison.
